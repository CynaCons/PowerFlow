# RadEAU — PowerFlow audit, 2026-09-19

Produced by `powerflow-audit` (`skills/powerflow-audit/scripts/audit.py --root ../RadEAU --tasks`).
Input for the retrofit iteration in RadEAU's own PLAN.md (v0.2.2 here). Judgment pass
per the skill: the two-letter ID scheme and `docs/SRS_*.md` location are **decide**
items (a live corpus of 100+ IDs — record D<n> in RadEAU: keep or migrate), the rest is **adopt**.

```
audit: RadEAU
- [ ] audit (root, high): AGENTS.md missing -> powerflow-init stamps AGENTS.md; for PLAN.md use powerplan create_plan
- [ ] audit (agents, medium): no docs/agents/README.md (memories layout) -> stamp templates/docs/agents/README.md; powerflow-memory writes memories/<topic>.md
- [ ] audit (ci, medium): guards not in CI: check-plan, check-version -> vendor templates/scripts/*.mjs and run them in a `guards` job (D13)
- [ ] audit (plan, medium): no stale-proof `## Current Status` header (D2) -> powerplan append_prose: 'The current iteration is the last heading below that is not marked COMPLETE'
- [ ] audit (root, medium): CLAUDE.md missing -> stamp the shim (templates/CLAUDE.md) so Claude reads AGENTS.md
- [ ] audit (srs, medium): SRS location: docs/SRS_*.md -> canon is docs/srs/SRS-<feature>.md with docs/srs/README.md as the index (D1)
- [ ] audit (srs, medium): requirement ID scheme(s): XX-NNN two-letter prefix (RadEAU) -> canon is SRS-<FEAT>-NNN; a migration is a project decision (D1) — record it or keep the scheme and note it in AGENTS.md
- [ ] audit (pack, low): not stamped by powerflow-init (no .powerflow/init.json) -> retrofit: stamp missing files only (stamp keeps existing ones), then fix the findings above
- [ ] audit (srs, low): table columns ['ID', 'Requirement', 'Priority', 'Status', 'Acceptance criteria'] -> canon columns ['ID', 'Requirement', 'Rationale', 'Verification', 'Trace']; status lives in Trace, priority in PLAN.md
- [ ] audit (srs, low): allocator is docs/REQUIREMENT_IDS.md -> fold it into docs/srs/README.md's allocator table so one index owns tags, numbers and format
findings: 10 (high 1, medium 6, low 3)
```
